# AGENTS.md

このリポジトリは Python 中心の録画支援ツールとして開発します。説明、Issue、設計文書は日本語を基本とし、初心者エンジニアにも意図が伝わるようにしてください。

## 最優先原則

- プロダクトコンセプトと提供機能の品質を、既存コードの維持より優先する
- 現在のコードは学習用資産として扱い、改善のためであればリポジトリ管理下のコード、テスト、文書の削除、置換、互換性を壊す再設計を恒久的に許可する
- 破壊的変更では目的、影響範囲、移行方法、検証結果をIssueと文書に残す
- 破壊的変更の許可は実行時データには適用しない。`user_data/`、秘密情報、録画ファイル、SQLite DB、キュー、ログを削除、上書き、初期化してはならない
- 実行時データの形式を変更する場合は、バックアップ、移行、失敗時のロールバックを先に設計する

## プロダクト方針

- OBS Plugin と OBS WebSocket に依存する実装を追加しない
- 録画、検出、履歴、対戦記録、タイムライン、復旧、アップロード準備の責務を Python 側で分離する
- SQLite、キュー、認証情報を扱う場合は復旧安全性を優先する
- ゲーム素材、スクリーンショット、抽出テンプレートをコミットしない
- 直接アップロードとOAuthはロードマップで対象に含めるまで実装範囲を広げない。GUIはWindows向け中核操作面として維持する

## Issue先行原則

- `docs/roadmap.md` に基づく開発作業は、実装前に必ずGitHub Issueへ登録する
- Issueは1ステップ1Issueに近い、単独で完了判定できる粒度にする
- タイトルは `[V0.x.y] 作業内容` 形式にする
- 本文には「関連バージョン」「目的」「作業内容」「完了条件」を必ず記載する
- 可能な限り `version:0.x.y` ラベルと同じバージョンのGitHub Milestoneへ接続する
- APIや権限の制約でラベルまたはMilestoneを利用できない場合も、タイトルと本文によるバージョン追跡は必須とする
- 実装中に新しい作業が判明した場合は、範囲を黙って拡張せず、先に追加Issueを登録する

## バージョン運用

- バージョンは `メジャー.マイナー.Fix` の3整数、コード上では `X.Y.Z` 形式で管理する
- 現在の開発版は `V0.17.0` とする
- 中核機能を1つ完了するごとにマイナーバージョンを更新し、Fixを `0` に戻す
- `main` への通常pushは、直前の `main` からFixを1つ増やしたバージョンを含める
- 中核機能の完了を反映するpushでは、Fix更新よりマイナー更新を優先して `X.Y+1.0` とする
- すべてのバージョン変更を `docs/release-notes.md` に記載する
- `pyproject.toml` の `project.version` と `master_duel_recorder_lite.__version__` を同時に更新する
- `V1.0.0` は全中核機能が揃い、ユーザーが明示的に「V1.0.0に変更せよ」と依頼した場合のみ設定する。エージェントの判断だけでは更新しない

## コード方針

- 読みやすさを優先する
- 不要な抽象化を避ける
- 初心者向けに必要なコメントだけを短く書く
- DB処理、録画処理、検出処理、アップロード準備を混ぜない
- 外部プロセス、ファイル、DBの失敗を正常完了として扱わない
- 変更のリスクに応じた単体テスト、統合テスト、復旧テストを追加する

## ドキュメント方針

仕様変更や設計判断は `docs/` に残してください。特に録画方式、保存先、復旧処理、ユーザー設定に関わる変更は、実装前に短い設計メモを追加します。ロードマップの進行状況とリリースノートは、実装およびバージョン変更と同じ作業内で更新します。
