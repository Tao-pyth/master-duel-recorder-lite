# V2.7.3: 自動監視開始直後停止不能Hotfix

V2.7.3では、V2.7.2実機検証で確認した、自動監視開始直後に停止できない問題を修正する。

## 修正内容

- `watch_starting`中に自動監視停止を許可するようにした。
- `watch_starting -> stopping -> idle`の状態遷移を正規の停止経路として扱うようにした。
- PySide6録画画面で、自動監視開始中でも停止ボタンと自動監視停止ボタンが使えるようにした。
- `stop_watch()`を自動監視開始直後に呼んでも不正状態遷移にならないようにした。

## 背景

V2.7.2公開後の実機検証で、手動録画開始・停止は復旧していた。一方、自動監視開始直後は`watch_starting`状態のまま停止操作が無効になり、直接停止しても`watch_starting -> stopping`が不正遷移として失敗していた。

## 互換性

- SQLite schemaは変更しない。
- 設定schemaは変更しない。
- 録画方式、FFmpegコマンド、保存先生成は変更しない。
- 既存録画ファイル、queue、manifest、OAuth資格情報は変更しない。
- OBS PluginとOBS WebSocketへの依存は追加しない。

## 検証

- Ruff: PASS
- 状態機械単体テスト: PASS
- PySide6 GUI契約テスト: PASS
- 録画サービス対象テスト: PASS
- 全pytest: PASS、616 passed、3 skipped
- PySide6録画画面smoke: PASS
- Windows CLI/GUI/updater EXE build: PASS
- 3 EXE smoke: PASS
- 実機GUI自動監視開始直後停止検証: PASS

## 公開状態

この文書作成時点では、`v2.7.3`のpush、tag、GitHub Release、artifact公開、公開後asset検証、Issue/Milestone closureは未実行である。実行にはユーザーの明示承認を必要とする。

## 関連

- Validation: [docs/validation/2.7.3.md](../validation/2.7.3.md)
- Release Contract: [docs/release-contracts/2.7.3.md](../release-contracts/2.7.3.md)
