# V1.4.1: YouTube一般配布導線hotfix

V1.4.1では、V1.4.0検証で見つかったGUI回帰とYouTube配布導線の不足を修正しました。YouTube OAuth client_idを配布EXEへ同梱できるようにし、未設定ビルドでは連携開始を無効化します。MP4準備は独立した主要ナビではなく、録画履歴からのYouTube投稿フロー内で投稿前処理として扱います。

## 主な変更

- 改善ページの`list_history()`引数不整合を修正
- 配布EXEへYouTube OAuth client_idを同梱するビルド経路を追加
- releaseビルドでOAuth client_id未設定を検出
- OAuth client_id未設定時のGUI状態表示と操作可否を整理
- `MP4準備`と`改善`をGUI主要ナビから外し、必要機能は既存導線へ統合
- 投稿ダイアログで投稿用MP4準備状態を表示
- `client_secret`なしのPKCE/desktop OAuth資格情報を保存、読み戻し、refreshできるように調整

## 検証

- `python -m ruff check .`
- `python -m unittest discover -s tests`、531 tests、3 skipped
- `python -m master_duel_recorder_lite --version`
- `python -m master_duel_recorder_lite.gui --smoke-test --smoke-output build\gui-smoke-1.4.1.json`
- `python scripts\verify_release_tag.py v1.4.1`
- `python scripts\build_windows_exe.py`
- `scripts\smoke_windows_exe.ps1 -ExePath dist\master-duel-recorder-lite.exe -ExpectedVersion 1.4.1`
- `scripts\smoke_windows_gui.ps1 -ExePath dist\master-duel-recorder-lite-gui.exe -ExpectedVersion 1.4.1`
- `python scripts\build_windows_exe.py --require-youtube-oauth-client`、未設定時にビルド前失敗

## SHA-256

- `master-duel-recorder-lite.exe`: `558E74835F97CDE2A9431DF00DE56E614B2C7D28F982960E8E951DFCE118C864`
- `master-duel-recorder-lite-gui.exe`: `08483D56FA8A91DA0A895D0C8C14440F4576AE3468C0CF5347EC2A6EC646638D`

## 未完了の外部ゲート

- 実YouTube private投稿E2Eは未実施
- 配布者管理OAuth Clientのclient_id、Google OAuth同意画面、テストユーザーまたは公開状態の確認が必要
- 公開release用EXEはOAuth client_id同梱後に再ビルドする必要がある
- 上記が完了するまで、タグ作成、GitHub Release、公開配布は行わない

詳細は[リリースノート](../release-notes.md)と[検証記録](../validation/1.4.1.md)を参照してください。
