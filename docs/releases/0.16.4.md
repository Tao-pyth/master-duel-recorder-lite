# master-duel-recorder-lite v0.16.4

WindowsでFFmpeg子プロセスが`0xc0000142`により一時的に初期化失敗し、自動監視の開始前診断が停止する問題を軽減するリリースです。

## 主な修正

- FFmpeg子プロセスを非表示で起動し、OSエラーダイアログを抑止
- `0xc0000142`だけを200ミリ秒後に1回再試行
- 能力検査と録画前フレーム取得へ同じ方針を適用
- 開始前診断の失敗項目と理由をGUIアクティビティへ表示

通常のFFmpegエラーは再試行しません。FFmpeg本体、設定、既存録画、履歴DBは変更しません。
