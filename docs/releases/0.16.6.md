# master-duel-recorder-lite v0.16.6

自動監視中の対戦開始待機メッセージを、経過秒付きの1行へ集約するリリースです。

## 主な修正

- `対戦開始を判定中です (4s)`形式で待機時間を表示
- 同じ待機状態は新規行を増やさず既存行を更新
- 対戦開始、状態変更、監視停止時に古い待機行を除去
- 録画開始、停止、エラーなど他のアクティビティ履歴は維持

対戦開始の判定条件、FFmpeg、設定、既存録画、履歴DBは変更しません。
