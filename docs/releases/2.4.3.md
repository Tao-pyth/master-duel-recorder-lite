# V2.4.3: レビュークリップ出力Hotfix

V2.4.3では、レビュー画面の「選択位置をクリップ出力」で、FFmpegが一時出力ファイルの形式を判定できず失敗する問題を修正した。

## 変更内容

- `ClipExportService` がFFmpegへ渡す一時出力ファイル名を `.partial.mp4` 形式に変更した。
- FFmpegへ渡す出力パスが `.mp4` 末尾になることを回帰テストで固定した。
- 最終出力ファイル名は従来どおり `.mp4` とした。
- 元録画を変更しない既存の非破壊契約と、成功時だけ最終ファイルへ置換する契約を維持した。

## 互換性

- SQLite schema、設定形式、録画ファイル、YouTube投稿履歴、OAuth資格情報、prepare queue、manifestは変更しない。
- Review画面とCLIのクリップ出力入口は変更しない。

## 追跡

- Issue: #595
- Validation: [docs/validation/2.4.3.md](../validation/2.4.3.md)
- Release Contract: [docs/release-contracts/2.4.3.md](../release-contracts/2.4.3.md)
