# 誤配置フォルダから統合したレガシー資産

このディレクトリは、2026-08-20に誤配置先 `D:\takuy\Documents\master-duel-recorder-lite` から確認された未統合資産を保管します。

誤配置先の作業ツリーは `0.8.0` 時点の未コミット資産を含んでいましたが、現在の正しい作業ツリーは `1.0.1` です。特に復旧CLI、復旧サービス、`recovery_artifacts` は現行のV0.19.0以降で撤去済みの設計と衝突します。そのため、これらは実行対象の `src/` と `tests/` へ戻さず、履歴参照用のレガシー資産として隔離します。

## 統合方針

- 現行 `1.0.1` のコード、DBスキーマ、テスト契約を優先する
- `user_data/`、録画ファイル、SQLite DB、キャッシュ、ビルド成果物は統合しない
- 古いPythonコードとテストは `.py.txt` として保存し、pytestやruffの対象にしない
- 復旧機能を再導入する場合は、この資産を直接戻さず、現行DBスキーマとロードマップに基づく新規Issueから再設計する

## 保管内容

- `architecture/`: V0.8.0時点の内部デザインノートと復旧設計メモ
- `src/`: V0.8.0時点の復旧関連モジュール
- `tests/`: V0.8.0時点の復旧関連テスト
