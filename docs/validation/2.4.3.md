# V2.4.3 Validation

## Summary

V2.4.3は、レビュー画面のクリップ出力でFFmpegが一時出力ファイルの形式を判定できず失敗する問題へのHotfixである。

このリリースはGitHub Release作成までは完了したが、Windows EXE release workflowの公開asset検証がGitHub APIの無認証rate limitで失敗したため、正式な完了版としては `V2.4.4` に置き換える。

## Release Workflow

| Item | Result | Notes |
| --- | --- | --- |
| GitHub Actions `32868991118` | Failed | `Verify published release assets` が `HTTP Error 403: rate limit exceeded` で失敗 |
| GitHub Release `v2.4.3` | Published | 資産は公開されたが、workflow完了条件は未達 |
| Replacement | V2.4.4 | Release asset検証を認証対応にして再リリースする |

## Automated Tests

| Command | Result | Notes |
| --- | --- | --- |
| `python -m pytest tests/test_clip_export.py tests/test_review_viewmodel.py tests/test_application.py` | Pass | 45 passed |
| `python -m ruff check src/master_duel_recorder_lite/clip_export.py tests/test_clip_export.py` | Pass | All checks passed |
| `python -m ruff check src tests scripts` | Pass | All checks passed |
| `python scripts/verify_release_tag.py v2.4.3` | Pass | `2.4.3` |
| `python -m pytest` | Pass | 597 passed, 3 skipped |
| `python -W error::ResourceWarning -m unittest discover -s tests` | Pass | 598 tests, 3 skipped |

## Build And EXE Smoke

| Command | Result | Notes |
| --- | --- | --- |
| `python scripts/build_windows_exe.py` | Pass | CLI/GUI/updater EXEを生成 |
| `./scripts/smoke_windows_exe.ps1 -ExePath ./dist/master-duel-recorder-lite.exe -ExpectedVersion 2.4.3` | Pass | CLI EXE smoke |
| `./scripts/smoke_windows_gui.ps1 -ExePath ./dist/master-duel-recorder-lite-gui.exe -ExpectedVersion 2.4.3` | Pass | GUI EXE smoke |
| `./scripts/smoke_windows_updater.ps1 -ExePath ./dist/master-duel-recorder-lite-updater.exe -ExpectedVersion 2.4.3` | Pass | updater EXE smoke |

## Local Artifact SHA-256

| Artifact | SHA-256 |
| --- | --- |
| `dist/master-duel-recorder-lite.exe` | `94b1b9a7dc2d7f8d8b386db9c23e6c8a8c6b8247a38b90fcfe93c9a30d4fc465` |
| `dist/master-duel-recorder-lite-gui.exe` | `8bf9518985a5e212ca307dc42976e93acb5a60720bfe0cbc43bdeb463083375c` |
| `dist/master-duel-recorder-lite-updater.exe` | `bdc38a716f2f7e238cd4536ef50e32c5d50cea3abea5894de93dd07acfd2ed87` |

## Acceptance Criteria

- [x] FFmpegへ渡す一時出力パスが `.mp4` で終わる。
- [x] 最終出力ファイルも `.mp4` で作成される。
- [x] 失敗時に元録画を変更しない既存契約を維持する。
- [x] `tests/test_clip_export.py` に一時出力名の回帰テストを追加する。
- [x] `python -m pytest tests/test_clip_export.py tests/test_review_viewmodel.py tests/test_application.py` が成功する。
- [x] `python -m pytest` が成功する。
- [x] `python -m ruff check src tests scripts` が成功する。
- [ ] Windows EXE release workflowで公開資産検証とダウンロード後スモークが成功する。

## Compatibility

- SQLite schema変更なし。
- 設定形式変更なし。
- 録画ファイル、YouTube投稿履歴、OAuth資格情報、prepare queue、manifest変更なし。
- 元録画非破壊と一時出力から最終出力への置換契約は維持。
