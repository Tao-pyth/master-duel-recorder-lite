# V2.7.11 検証記録

## 自動検証
- `python -m ruff check src tests scripts`: 合格。
- `python -W error::ResourceWarning -m unittest discover -s tests`: 640件、3 skipped、失敗なし。
- `test_pyside_season_report.py`: 実widgetで2戦1勝・0戦・比較あり/なし・少数標本・日本語メモのplain text表示・日別累積値・読み取り専用を確認。
- 未選択/取得失敗の案内、選択ID固定、異なるIDの拒否、メモ不変を確認。
- HTML保存直前の再集計、新規保存、上書き拒否/許可、取消、不正拡張子、取得/保存失敗と既存出力保護を確認。
- バージョン整合検証と `git diff --check`: 合格。

## 通常GUI
専用データで通常起動したGUIのシーズン「レポート」ボタンから表示。12戦8勝、比較対象、保存済みメモ、0戦、全5タブ、740×480まで縮小した操作部、実際のHTML保存を8画像に記録し、全画像のSHA256を照合した。DPR1.0、通常サイズ920×640。小さい画面でも再集計/HTML保存/閉じるへ到達し、長い保存先は折り返す。利用者の既存データは使用しない。画像はリポジトリへコミットしない。

実ゲーム・長時間録画の検証はこの閲覧/出力変更の対象外。比較対象選択、メモ編集、他画面の入口は今回追加していない。

## 公開
正式公開と配布検証は[v2.7.11 Release](https://github.com/Tao-pyth/master-duel-recorder-lite/releases/tag/v2.7.11)および同タグのWindows EXE release workflowを正本とする。3EXE build/smoke、公開7asset hash、再取得3EXEsmokeの完了を確認してIssue #641とMilestone #93を閉じる。
