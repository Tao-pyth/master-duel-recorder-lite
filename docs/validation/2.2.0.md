# V2.2.0 Validation

Date: 2026-08-23

## 対象

V2.2.0 PySide6 UI読み取り品質改善。

## 検証結果

| Check | Result | Notes |
| --- | --- | --- |
| `python -m pytest tests/test_pyside_gui.py tests/test_v201_gui_recovery.py` | Pass | 12 passed。UI usability contract、カレンダー、統計チャート、表可読性、カラー表示契約を確認 |
| `python -m master_duel_recorder_lite.pyside_gui --smoke-test --smoke-page statistics --smoke-output build\\pyside-ui-usability-smoke.json --smoke-screenshot build\\pyside-ui-usability-smoke.png` | Pass | 統計画面のカレンダー入力、タブ、棒+線グラフ領域を確認 |
| `python -m master_duel_recorder_lite.pyside_gui --smoke-test --smoke-page history --smoke-output build\\pyside-ui-history-smoke.json --smoke-screenshot build\\pyside-ui-history-smoke.png` | Pass | 戦績管理表の列幅、横スクロール、デッキ色見本、淡い選択色を確認 |
| `python -m master_duel_recorder_lite.pyside_gui --smoke-test --smoke-page decks --smoke-output build\\pyside-ui-decks-smoke.json --smoke-screenshot build\\pyside-ui-decks-smoke.png` | Pass | デッキ名管理のカラー列スウォッチ、列幅、淡い選択色を確認 |
| `python -m ruff check .` | Pass | 静的検査合格 |
| `python -m pytest` | Pass | 587 collected、584 passed、3 skipped |

## UI確認観点

- `build/pyside-ui-usability-smoke.png` を目視確認し、統計画面の開始日・終了日が `yyyy-MM-dd` の日付入力で、カレンダーを開ける入力部品として表示されることを確認した。
- `build/pyside-ui-usability-smoke.png` を目視確認し、統計推移が棒と線のグラフ領域として表示され、0件時も空白にならず「表示できる確定済み対戦がありません」と表示されることを確認した。
- `build/pyside-ui-history-smoke.png` を目視確認し、戦績管理表の主要列に幅の意図があり、横スクロールとデッキ色見本が表示されることを確認した。
- `build/pyside-ui-history-smoke.png` と `build/pyside-ui-decks-smoke.png` を目視確認し、表の選択色が淡く、選択中も文字と色見本を読めることを確認した。
- `build/pyside-ui-decks-smoke.png` を目視確認し、デッキ名管理のカラー列に色見本が表示されることを確認した。

## Smoke Contract Snapshot

```text
version: 2.2.0
ui_usability_contract: True
calendar_picker_contract: popup_calendar=True, display_format=yyyy-MM-dd
statistics_chart_contract: visual_type=bar_and_line
table_readability_contract: horizontal_scroll=True, explicit_column_widths=True
color_swatch_contract: history_deck_decoration=True
```

## データ保護確認

- DB schemaは変更しない。
- 設定形式は変更しない。
- 録画ファイル、queue、manifest、OAuth資格情報は削除、初期化、形式変更しない。
