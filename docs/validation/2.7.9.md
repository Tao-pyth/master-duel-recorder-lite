# V2.7.9 検証記録

## 自動検証

- Ruff: `python -m ruff check src tests scripts` 合格。
- 全体: `python -W error::ResourceWarning -m unittest discover -s tests`、633件、3 skipped、失敗なし。
- 新規回帰: `test_pyside_audit_fixes.py`。隔離DBで統計全体/条件適用後、月別、期間指定0件、逆転日付、設定の未保存保持・保存失敗・終了取消・保存成功、未完了抽出と状態列、980/1180幅の実widgetを検証。
- 既存録画レイアウト回帰: 4サイズ×6録画状態と各操作の接続を維持。

## 通常起動の画面

Windows/PySide6の通常起動を専用DBで実施。8画面、設定7タブ、主要ダイアログ、統計条件変更をキャプチャした。最終確認ではv2.7.9を表示、DPR 1.5（980論理pxは1470画像px）、1180×760と980×640を使用。

| 対象 | 結果 |
| --- | --- |
| F1 統計要約 | 全体・条件適用後8勝/10戦、先攻/後攻80% |
| F2 条件 | 敗北のみ0勝/2戦、2030年1月は0件と空グラフ |
| F3 録画 | 980幅で状態・開始・停止・自動監視の全幅を同時表示 |
| F4 設定 | 共通保存/再読込が狭い画面でも表示され、タブ内容だけスクロール |
| F5 戦績管理 | 操作2行、表へ高さを配分、手動追加へ統合 |
| F6 未完了 | 編集中2行へ絞り込み、状態を開始日時の隣へ表示 |
| F7 グラフ | 勝利数/勝率の軸目盛りと推移の数値表 |
| F8 文言 | 通常起動文言と録画設定/自動監視・診断の名称 |

画像は実行環境の証跡フォルダーへ保存し、ゲーム素材・ユーザーデータとしてリポジトリへ含めない。実ゲーム録画、実投稿、長時間負荷、スクリーンリーダーの検証を行ったという意味ではない。

## 配布・公開

GitHub Actionsが3EXEをビルドし、smoke、7asset公開、公開SHA256照合、再取得後smokeを実施する。公開完了の正本は[v2.7.9 Release](https://github.com/Tao-pyth/master-duel-recorder-lite/releases/tag/v2.7.9)と同タグのWindows EXE release workflow。Issue #631〜#638とMilestone V2.7.9は公開検証完了後に閉じる。
