# V2.1.1 Validation

Date: 2026-08-23

## 対象

V2.1.1 PySide6 rich UI回帰修正。

## 検証結果

| Check | Result | Notes |
| --- | --- | --- |
| `python -m pytest tests/test_v201_gui_recovery.py tests/test_version.py` | Pass | 10 passed。rich baseline contract、入口切替、既存データ再読込、バージョン整合を確認 |
| `python -m pytest` | Pass | 587 collected、584 passed、3 skipped |
| `python -m master_duel_recorder_lite.pyside_gui --smoke-test --smoke-output build\\pyside-rich-smoke.json --smoke-screenshot build\\pyside-rich-smoke.png` | Pass | PySide6 GUI起動、smoke JSON、スクリーンショット生成を確認 |
| `python -m ruff check .` | Pass | 静的検査合格 |

## UI確認観点

- 録画対象、録画状態、録画なし戦績追加、環境診断、アクティビティが録画画面に表示される。
- 戦績管理表、統計タブ、デッキ/タグ/シーズン編集、テンプレート編集、設定タブが表示される。
- `docs/assets/tkinter-ui-baseline-1.5.2-rich` の主要9画面と内部2画面を、PySide6側の画面構成の基準として扱う。
- `build/pyside-rich-smoke.png` を確認し、録画画面が画像1のような簡略UIではなく、画像2同等の情報ブロックを持つことを確認した。

## Smoke Contract Snapshot

```text
version: 2.1.1
pyside6: True
gui_entrypoint: master_duel_recorder_lite.pyside_gui
standard_feature_contract: True
standard_operation_contract: True
rich_ui_baseline_contract: True
widget_count: 69
failed_count: 0
missing_count: 0
internal_pages: prepare, improve
rich_baseline_assets: 11
```

## データ保護確認

- DB schemaは変更していない。
- 設定形式は変更していない。
- 録画ファイル、queue、manifest、OAuth資格情報は削除、初期化、形式変更していない。
- PySide6 smokeではruntime dataを作らない契約を維持する。
