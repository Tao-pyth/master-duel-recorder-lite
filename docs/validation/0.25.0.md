# V0.25.0検証記録

状態: ローカル実装検証済み・先行バージョン公開待ち

## 自動検証

2026-08-15時点で次を確認しました。

- 統計ページと同じ母集団・同じ期間条件でシーズン集計が一致する
- 同種別の直前開始シーズンを既定候補とし、期間重複、比較なし、母数0を安全に扱う
- 安定デッキID・カラーを維持して全体・先攻・後攻・未設定を返す
- 表裏、先後、最終勝敗の未知値を推測せず分離する
- 開始日・終了日を含む日・週の空区間と使用デッキ比率を補完する
- JSTの日付境界を固定タイムゾーンで再現する
- 振り返りの文字数・制御文字・revision競合と明示アーカイブを検証する
- HTMLエスケープ、印刷CSS、外部依存・絶対パスなし、上書き、Windows予約名、原子的保存失敗を検証する
- 未参照シーズンの完全削除と参照中シーズンのアーカイブ前に、検証済みバックアップを作成する

- Ruff: `python -m ruff check .` 成功
- 単体・統合テスト: `python -m unittest discover -s tests` 435件実行、2件skip
- Windows CLI/GUI EXEビルド成功
- CLI EXE隔離スモーク成功、ProductVersion `0.25.0`
- GUI EXE隔離スモーク成功、ProductVersion `0.25.0`
- GUIスモークで戦績管理の履歴更新がウィンドウ内に収まり、カレンダーの矢印幅と当月復帰ボタン状態が契約どおりであることを確認
- 自動録画スナップショットがFFmpegセッションの開始時刻を保持し、500ms周期のGUIポーリングごとに経過時間を更新することを単体テストで確認
- 監視ループ統合テストで、境界停止、同一候補による次録画開始、診断遷移、監視終了時の新録画停止、操作状態の待機復帰を一続きで確認
- 高特異度のウルトラワイド下段LOSEだけを短時間結果として許可し、補足有効11戦の2fps全区間評価で開始11/11、盤面11/11、結果11/11、各Precision/Recall 1.000を確認
- 補足負例3本では開始・盤面・結果の誤検出0。非対戦録画の`match_error`だけを1件検出
- 現存14本をID・サイズ・SHA-256で固定した`docs/validation/0.25.0-runtime-corpus.json`を正式な超横長回帰コーパスとして採用
- 1920x1080ウィンドウ4本を`docs/validation/0.25.0-window-corpus.json`へ固定し、標準16:9の開始・盤面・結果2/2、負例2本の検出イベント0を確認
- 標準16:9の正規LOSE見逃しを、中央白文字形状と敗北4/5合意で修正
- シーズンレポートを1180x760、980x640で実描画し、980x640では概要、デッキ・先後、コイントス、推移、振り返りの全タブで領域外配置0件を確認
- CLI EXE: 9,422,091 bytes、SHA-256 `04CC09D236756B8EC7C9E7D555367115E09250A1BC7E2C7AF97D9ADB8BED5E3A`
- GUI EXE: 13,074,047 bytes、SHA-256 `4DDB2E8C92DEB4D34891955A733BE1E724ACB6CC1B18E836B1A77F21F86FB33B`

GitHub ActionsのWindows CIは、先行バージョンの公開順序を満たして`main`へpushした後に確認します。

旧提供動画14本の復元は困難なため、特定パスへの復元を公開条件から除外しました。代替コーパスは3440x1440フルスクリーン14本と1920x1080ウィンドウ4本です。ウィンドウ2戦は盤面途中から始まるため、16:9のコイントス、リプレイ、マッチエラーが最新実動画で未評価である点を既知リスクとして扱います。

## 2026-08-15実戦ログ再評価

`user_data/logs/visual-monitor/`の4セッションを`validate_live_monitoring.py`で再評価しました。生成物は`build/validation/live-20260815.json`と`build/validation/live-20260815.md`です。

- 評価対象: 7試行、合格4、未達3、最大連続成功2、最新連続成功0/3
- 第3戦の1試行は結果停止後も盤面が継続しており、決着前停止として不合格
- 残る2試行は結果停止イベントがないまま診断セッションが終了した。既存ログでは手動の監視停止と結果検出失敗を区別できないため、検出失敗とは断定しない
- ストリーム再起動0、壊れたイベント0、候補破棄0
- 旧診断が1秒間引き後の保存件数を経過時間で割り、900件到達後は分子が増えない実装だったため、最小0.646・平均0.827という旧ログの実効fps値は無効な参考値と判明した。成功・停止遷移とスコア時系列の評価は影響を受けない
- マッチエラーとオーバーレイは観測済み、リプレイは未観測

今後のログでは`watch_stopped_with_active_recording`を記録し、利用者による監視停止と結果判定による停止を区別します。実戦集計CLIでも中断試行として別集計し、失敗戦と連続成功試験の対象には含めません。実効fpsは間引き前の全解析回数で計測し、待機周期はフレーム取得・解析時間を差し引きます。判定閾値は、この2試行の終了理由を識別できない状態では変更しません。

## 2026-08-15追加2セッション

利用者から申告された5件を確認したところ、SQLiteには2セッション・6録画が記録されていました。前半セッションは4録画すべて`result_stopped`、後半セッションは2録画のうち1件が`result_stopped`、1件が`boundary_stopped`でした。後半1件は結果を検出できないまま次対戦の`READY`まで録画を継続し、その次の録画は開始演出から始まっており、同じ次対戦が2ファイルへ分断されていました。境界検出から再検出開始まで約8秒の欠落があるためIssue #366を登録しました。

現行集計器で2セッションを再評価した結果は6試行中5件合格、境界停止1件不合格、最大連続成功4、最新連続成功1/3、実効fps最小1.941・平均1.944でした。境界停止後に次候補を確認できているため監視復帰は成功していますが、前対戦の結果停止条件を満たしていないため公開ゲートは未達です。

fix340では境界候補を前録画の停止だけで消費せず、時間窓合意が成立した同じ監視ループで前録画を停止し、同じ画面座標と候補から次録画を開始します。`boundary_stopped`には境界時のスコア・合意を、`boundary_handoff_started`には引継ぎ時間を保存します。前対戦の結果見逃しは実戦ゲート上の失敗として維持し、次対戦の引継ぎ成功とは分離して集計します。

## fix340 初期3戦

正式な初期確認セッション`20260815T111812Z-6dc10144`を確認しました。2026-08-15 20:18:12 JSTに開始し、3戦すべてで候補開始、盤面確定、結果停止、監視復帰が成立しました。結果停止後の盤面継続、候補破棄、利用者中断はありません。最初の盤面確定前にストリーム再起動が2回発生しましたが自動復旧し、実効fpsは最小1.945、平均1.948でした。

- 実戦ゲート: 開始3/3、盤面3/3、結果停止3/3、監視復帰3/3
- 録画ファイル: 3/3が`completed`、非空、FFmpeg終了コード0
- 録画時間: 374.9秒、622.0秒、765.9秒
- 終了映像: 3/3で決着後の報酬・遷移画面を確認し、攻撃演出中の早期停止なし
- 直前の補足セッション`20260815T105350Z-1da98948`: 2/2合格。fix340の新規取得分は合計5/5合格

今回の5戦では前対戦の結果見逃しがなく、`boundary_handoff_started`は発生していません。Issue #366の境界引継ぎ経路は監視ループ統合テストでは確認済みですが、実戦では引き続き未観測として追跡します。初期3戦ゲートは合格とし、次は別セッションで10戦連続試験を実施します。

## 公開順序

V0.23.0のオフラインゲートは合格しましたが、実戦ゲートと、それに続くV0.24.0公開が未完了です。V0.25.0はローカル実装を進めますが、先行版より先に`main`へpush、タグ、GitHub Releaseを公開しません。

追跡: [V0.25.0 Milestone](https://github.com/Tao-pyth/master-duel-recorder-lite/milestone/47)、Issue #315 - #327、#333 - #339、#366
