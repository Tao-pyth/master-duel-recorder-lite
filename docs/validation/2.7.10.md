# V2.7.10 検証記録

## 自動検証
- `python -m ruff check src tests scripts`: 合格。
- `python -W error::ResourceWarning -m unittest discover -s tests`: 637件、3 skipped、失敗なし。
- `test_audio_settings_integrity.py`: 候補の順序変更/重複/消失/再出現/0件/列挙失敗、保存済み・未保存選択、保存/再読込を実widgetで確認。
- device/systemへの切替とnoneへの復帰をキー順の両方向で保存。不正値・未知キー・無効最終状態と保存呼出失敗で現config/.previous不変を確認。
- 最終os.replaceのPermissionErrorで現configが不変、.previousに有効な現設定のバックアップが残り、再読込できることを確認。保存方式は既存契約を維持。
- 単一項目updated_configの無効なdevice切替を引き続き拒否。

## 通常GUI
v2.7.10を専用データで通常起動し、音声候補の列挙結果だけをfixtureへ置換した。1180×760と980×640、DPR1.5で5画像を採取。未検出案内、保存済み表示、未保存表示、選択保持、共通保存ボタンの到達性を確認。ゲーム素材はコミットしない。実録音・実投稿・長時間負荷の評価は含めない。

## 公開
正式公開と配布検証は[v2.7.10 Release](https://github.com/Tao-pyth/master-duel-recorder-lite/releases/tag/v2.7.10)および同タグのWindows EXE release workflowを正本とする。3EXE build/smoke、7asset hash、再取得3EXEsmokeを行い、完了後にIssue #639/#640とMilestoneを閉じる。
